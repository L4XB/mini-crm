// Code generated by Mini CRM SDK - DO NOT EDIT.
// Generated at: {{timestamp}}

package routes

import (
	"github.com/deinname/mini-crm-backend/controllers"
	"github.com/deinname/mini-crm-backend/middleware"
	"github.com/gin-gonic/gin"
)

// RegisterCustomRoutes registers all custom model routes
func RegisterCustomRoutes(router *gin.RouterGroup) {
	// Protected group with auth middleware
	protected := router.Group("/")
	protected.Use(middleware.AuthMiddleware())

	{{#each models}}
	// {{name}} routes
	{{camelCase name}}Controller := controllers.New{{name}}Controller()
	{{camelCase name}}Routes := protected.Group("/{{tableName}}")
	{
		{{camelCase name}}Routes.POST("/", {{camelCase name}}Controller.Create{{name}})
		{{camelCase name}}Routes.GET("/", {{camelCase name}}Controller.Get{{name}}s)
		{{camelCase name}}Routes.GET("/:id", {{camelCase name}}Controller.Get{{name}})
		{{camelCase name}}Routes.PUT("/:id", {{camelCase name}}Controller.Update{{name}})
		{{camelCase name}}Routes.DELETE("/:id", {{camelCase name}}Controller.Delete{{name}})
	}
	{{/each}}
}
