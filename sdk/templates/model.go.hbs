// Code generated by Mini CRM SDK - DO NOT EDIT.
// Generated at: {{timestamp}}

package models

import (
	"time"

	"gorm.io/gorm"
)

// {{model.name}} represents the {{model.name}} entity
type {{model.name}} struct {
	gorm.Model
	{{#each model.fields}}
	{{#unless (eq name "id")}}
	{{#unless (eq name "createdAt")}}
	{{#unless (eq name "updatedAt")}}
	{{#unless (eq name "deletedAt")}}
	{{pascalCase name}} {{goType type}} `json:"{{camelCase name}}" gorm:"{{#if unique}}unique;{{/if}}{{#if required}}not null;{{/if}}"`
	{{/unless}}
	{{/unless}}
	{{/unless}}
	{{/unless}}
	{{/each}}
}

// TableName overrides the table name
func ({{model.name}}) TableName() string {
	return "{{model.tableName}}"
}

// Validate validates the {{model.name}} model
func (m *{{model.name}}) Validate() error {
	// Implement validation logic here
	return nil
}

// BeforeCreate hook called before creating a record
func (m *{{model.name}}) BeforeCreate(tx *gorm.DB) error {
	return m.Validate()
}

// BeforeUpdate hook called before updating a record
func (m *{{model.name}}) BeforeUpdate(tx *gorm.DB) error {
	return m.Validate()
}
